{
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.1",
    "pg": "^8.13.0",
    "ws": "^8.18.0"
  },
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "setup": "psql -U postgres -f setup.sql",
    "dev": "psql -U postgres -f setup.sql && node --watch server.js"
  }
}
